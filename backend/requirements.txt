PyMuPDF>=1.24
pytesseract>=0.3.10
Pillow>=10.3
sentence-transformers>=3.0
chromadb>=0.5
groq>=0.9
python-dotenv>=1.0
pytest>=8.0

# --- Added for hybrid retrieval / multi-format support ---
rank_bm25>=0.2.2        # BM25 lexical retrieval (feature 1)
# NOTE: cross-encoder re-ranking (feature 2) uses sentence-transformers'
# built-in CrossEncoder class -- no separate package needed.
python-docx>=1.1        # DOCX ingestion (feature 12)
python-pptx>=0.6.23     # PPTX ingestion (feature 12)
openpyxl>=3.1           # XLSX ingestion (feature 12, optional format)

# --- Added for the React frontend migration (backend API server) ---
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9   # required by FastAPI for file uploads
